import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pathtracker.app',
  appName: 'Path Tracker',
  webDir: 'dist/app-frontend/browser'
};

export default config;
