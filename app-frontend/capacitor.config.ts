import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.scoutapp.app',
  appName: 'app-frontend',
  webDir: 'dist/app-frontend/browser'
};

export default config;
