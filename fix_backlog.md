# Fix Backlog

This file is for user-requested fixes, UX notes, bugs, and small product changes.

Codex should read this file before starting app changes and may read it again while working, because the user may edit it at any time.

## Pending
New requested changes here.

## Done
Move completed items here only when the user agrees or when the related change is clearly implemented.

- A completed task should not be crossed out only a bit graied out.
- There is a problem in the text boxes. The last line is partially hidden. (textarea)
- The journey title should be replaced with the active goal title.
- I don't like the goal title to be a filed like that. The title itslef should be modifiable when you click on it.
- Let's remove the status from goal and remove goal description. (the status can exist but only in the backend for archive visualization)
- For steps also no text box for it but editable. The idea is that it should feal like a note line.
- The tasks box should resize autoatically when I write new line(again it should feel like a notes app)
- The menu bar is too thin in the browser, it looks squashed. The icons are partially hidden.
